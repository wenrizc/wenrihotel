# 文档规范

## 1. 结论

本规范适用于 `D:\notes\docs\面经` 下所有 `.md` 笔记（含新建与修订）。

## 2. 适用范围

- 本规范适用于 `D:\notes\docs\面经` 下所有 `.md` 笔记（含新建与修订）。
- 每个文件对应一个问题或主题，标题与文件名一致。
- 新建条目前先检索是否已有同主题文件，避免重复。

## 3. 文件与编码

- 编码为 UTF-8（无 BOM）。
- 行尾使用 LF，不保留行尾空格。
- 文件名保持可读，修正明显错别字；不随意更改既有术语大小写。
- 文件名建议：`{问题}.md`，简短清晰，避免无意义前缀；必要时用 `-` 连接。
- 文件名中的常见缩写统一大小写：HTTP、HTTPS、TCP、UDP、DNS、URL、URI、IP、JVM、JDK、JUC、GC、SQL、MySQL、Redis、Kafka、RabbitMQ、RocketMQ、Netty、Nginx、ZooKeeper、CAS、AQS、AOP、MVC、IoC、JWT、ORM、RDB、AOF、TTL、QPS、RBAC、ABAC、etcd。
- 目录名中的常见缩写统一大小写：Java、Spring、Redis、Agent。
- 当术语有多种写法时，优先采用官方/社区主流写法（如 `MySQL`、`ZooKeeper`）。

## 4. 标题与层级

- 一级标题：`# {问题}`，文件首行且唯一。
- 二级及以下标题必须带编号：
  - `## 1. ...`
  - `### 1.1 ...`
  - `#### 1.1.1 ...`
- 标题文本不重复编号，不使用“第X章”等格式。
- 编号按层级递增，不跳号；同级标题保持并列结构。

## 5. 正文与段落

- 段落之间空一行；列表与段落前后空一行。
- 中文标点使用全角；英文缩写与数字使用半角。
- 关键结论、要点可用 **加粗**，但避免通篇加粗。
- 单段不宜过长，尽量 3–5 句以内，必要时拆分为列表。

## 6. 列表与表格

- 无序列表使用 `-`；有序列表使用 `1.`。
- 子列表缩进 2 个空格，层级不超过 3 级。
- 表格必须包含表头与对齐行，字段名使用名词短语。
- 表格适合“对比/归纳”，不放长段文字。

## 7. 代码与命令

- 使用围栏代码块，优先标注语言（如 `java`、`shell`、`sql`）。
- 行内代码使用反引号，术语/变量可用行内代码突出。
- 命令示例尽量给出输入与预期输出的最小片段。

## 8. 内容要求

- 必须给出问题的直接回答或结论（1–3 句）。
- 视主题补充：原理、步骤、优缺点、适用场景、注意事项、示例。
- 增加“常见追问/易错点/排查思路”等加分项，并给出完整、具体的答案。
- 术语统一，避免明显错别字和前后冲突；首次出现的缩写给出全称。
- 避免“只讲定义不落地”，需要能用一句话说明“怎么用/何时用”。

## 9. 可选结构模板

- `## 1. 结论`
- `## 2. 原理/流程`
- `## 3. 关键点/注意事项`
- `## 4. 优缺点/适用场景`
- `## 5. 常见追问/易错点（含完整答案）`
- `## 6. 示例/伪代码`

## 10. 写作自检清单

- [ ] 标题与文件名一致且唯一？
- [ ] 是否给出直接结论？
- [ ] 是否补充了原理或流程？
- [ ] 是否包含可扩展点（对比、场景、优化）？
- [ ] 是否有至少 1 条面试追问/误区？
- [ ] 是否为追问/易错点给出完整答案？
- [ ] 术语/缩写是否统一？

## 11. AI 提示词（用于生成/优化笔记）

- 目标：生成面试可用、结构化、可扩展的答案。
- 提示词模板：

  ```text
  你是一名资深面试官+技术讲师，请围绕「{问题}」输出 Markdown 笔记：
  1) 先用 1-3 句给出结论/核心回答
  2) 原理或流程（分步骤）
  3) 关键点/注意事项
  4) 优缺点/适用场景（对比同类方案）
  5) 常见追问/易错点（给出完整具体答案）
  6) 示例/伪代码（可选）
  要求：术语统一、逻辑清晰、避免空话，适度延伸到实践与排查思路。
  ```

## 12. 常见追问/易错点

- 追问：文档规范的核心流程或关键点是什么？
  - 答：核心结论是：本规范适用于 `D:\notes\docs\面经` 下所有 `.md` 笔记（含新建与修订）。展开时可按“适用范围、文件与编码、标题与层级”组织，先概述再逐点展开，保证结构完整。其中适用范围侧重本规范适用于 `D:\notes\docs\面经` 下所有 `.md` 笔记（含新建与修订），文件与编码侧重编码为 UTF-8（无 BOM）。回答时要体现步骤、关键点与适用场景，必要时补充示例或对比。
- 易错点：文档规范中最容易混淆或踩坑的点是什么？
  - 答：常见易错点是缺少示例导致落地性不足。比如适用范围中提到：本规范适用于 `D:\notes\docs\面经` 下所有 `.md` 笔记（含新建与修订）。文件与编码中还提到：编码为 UTF-8（无 BOM）。这些细节很容易被忽视。回答时应明确边界、关键步骤与适用场景，并用实例或对比验证。
