项目开发背景 现有主要是网页全部交付大模型理解 并且想要学习主流coding agent的开发逻辑
# Crawler-Agent 项目面试介绍（深度优化版）

该项目是一个基于 LangGraph 的多智能体爬虫生成系统，旨在解决复杂动态网页的自动化解析与代码生成难题。架构上，我采用了 **Supervisor 模式** 协调多个子 Agent，分别数据发现，字段映射，代码生成，并结合 **Playwright抓取网页 与多模态视觉模型理解网页内容** 突破了传统爬虫对动态渲染和隐含接口的识别瓶颈。为了解决大模型输出不稳定的核心痛点，我设计了 **基于 Pydantic 的“格式+逻辑”双重验证与熔断机制**，实现了 Agent 的自动纠错（Self-Correction），同时配合 **LangSmith** 实现了全链路的可观测性与数据驱动优化，最终将复杂爬虫任务的生成成功率提升至 85% 以上，显著降低了人工开发成本。

理想情况下，用户在 CLI 提交“目标站点 + 期望字段/Schema”等需求后，**主 Agent（Supervisor）**先进行需求理解并规划执行路径，然后通过 LangGraph 的条件边依次路由到各子 Agent：先由 **Data Source Discoverer** 访问页面/API、验证是否存在满足字段需求的数据源并把原始 JSON/HTML 写入 **DataCache**；接着 **Field Mapper** 仅从缓存读取原始数据（不再访问网络），为每个目标字段生成稳定的提取规则（如 JSONPath/CSS）；最后 **Code Generator** 基于站点信息与字段映射产出可执行的 Python 爬虫代码，过程中所有决策与结构化产物都追加到 LangGraph 的 `messages` 状态里便于追踪与回放，并可通过 LangSmith 对整条执行链进行可观测性分析


我做了一个“智能爬虫代码生成系统”：用户用自然语言描述要爬什么字段，我的系统会 **自动完成数据源发现 → 流量缓存 → 字段映射 → 生成可运行爬虫代码**。  
核心难点是任务链路长、LLM 输出不确定、网站差异大，所以我用 **LangGraph Supervisor 做多智能体编排**，并用 **Pydantic 作为子 Agent 的输出契约**：每一步产物都必须是可校验的结构化结果，校验失败就把 **错误 + schema** 返给子 Agent 自我修复，多次失败再明确报错给用户，保证稳定性与可维护性。

# 简历点一（重点升级版）：Supervisor 架构 + ReAct + “输出格式验证/自愈重试”
构建这个智能爬虫系统时，我面临的核心矛盾是：用户的需求是模糊的自然语言（例如“抓取某网站的商品价格”），而代码生成需要极其严谨的结构化信息（URL、选择器、字段类型）。这就要求系统既要有宏观的理解能力，又要有微观的执行精度。单一 Agent 很难同时处理好上下文记忆和具体的页面解析任务，容易出现上下文丢失或逻辑混乱。

### Task（任务）

我需要设计一个分层架构：

1. **主智能体（Orchestrator）**：充当“项目经理”，负责解析用户意图、维护全局状态、分发任务，并对子 Agent 的交付物进行验收。
    
2. **子智能体（Workers）**：专注特定领域（如发现页面、解析字段），并具备自我修正能力。
    
3. **状态流转**：确保信息在不同 Agent 间准确传递，无损耗。
    

### Action（行动）

**主 Agent 的核心职能设计** 我并没有把 Orchestrator 仅仅设计成一个路由器，而是赋予了它**规划者（Planner）**和**验收者（Reviewer）**的角色：

1. **意图解析与任务拆解**： 主 Agent 接收用户输入后，首先维护一个全局的 `SharedState`。它会分析需求，决定是先调用 `Discoverer` 去探查网页结构，还是直接调用 `CodeGenerator`。它保留了完整的对话历史，确保即使在多轮交互后，依然记得用户的原始目标。
    
2. **结构化指令分发**： 为了保证子 Agent 理解准确，主 Agent 将任务上下文封装成标准的 State 对象传递。例如，当从 Discoverer 切换到 FieldMapper 时，主 Agent 会确保“已发现的 URL 列表”和“HTML 缓存 Key”被正确写入状态字典。
    

**ReAct 循环与动态路由** 我利用 LangGraph 构建了一个循环图。主 Agent 根据当前状态决定 `Next_Step`。 如果子 Agent 完成任务但结果不完整（例如 FieldMapper 没找到所有字段），主 Agent 会分析返回的消息，决定是让子 Agent 重试，还是请求人工介入，而不是机械地向下执行。

### Result（结果）

这种设计将复杂的长链路任务拆解为独立的可控单元。主 Agent 成功扮演了状态管理者的角色，使得系统能够处理包含 10 轮以上交互的复杂爬虫需求，任务完成率从单 Agent 的 40% 提升到了 85% 以上，并且具备了处理中断和上下文恢复的能力。
## Situation（背景）

多步骤任务如果只用单一 Agent，很容易出现两类问题：  
1）步骤编排混乱：该做数据发现时开始写代码、该字段映射时还在猜数据源。  
2）LLM 输出不稳定：子 Agent 有时会输出“看起来合理但不可用”的半结构化内容，导致后续步骤接不起来，最后用户拿到的是不可运行方案。

## Task（任务）

我需要一个主控架构同时解决三件事：  
1）**主 Agent 能动态路由**：根据进度把任务派给合适的子 Agent。  
2）**子 Agent 具备 ReAct 自主推理**：遇到懒加载/反爬能调整策略，而不是一次性调用工具就结束。  
3）**关键：跨 Agent 的“结构化产物”必须可靠**：每个子 Agent 的输出要能被机器强校验，失败要能自愈重试，最终确保 Orchestrator 拼装出来的结果可用。

## Action（行动）

### 1）主 Agent（Orchestrator）我强调它的“职责边界”

面试时建议你把 Orchestrator 讲成一个“**产品级的调度/质检/汇总器**”，不是简单的路由器：

- **任务拆解与路由**：解析用户目标，决定下一步是 discover / map / generate
    
- **上下文治理**：控制每轮只给子 Agent 必要上下文（避免噪声导致跑偏）
    
- **产物质检（重点）**：对每个子 Agent 的输出做 Pydantic 验证，保证结构化正确
    
- **失败自愈**：验证失败→把“错误信息+schema+示例”喂回子 Agent 修复再输出
    
- **容错与停止策略**：重试次数上限、步数上限（is_last_step）、以及最终对用户的清晰报错
    
- **结果汇总**：把 discover/mapping/code 三段产物合并成用户可用的一份最终方案
    

### 2）子 Agent 输出契约：Pydantic 统一返回格式（关键亮点）

我给每个子 Agent 定义一个**明确的输出 schema**（Pydantic BaseModel），例如：

- DiscovererOutput：候选数据源列表、推荐数据源、证据（URL/接口/命中字段）
    
- FieldMappingOutput：字段→JSONPath/CSS、样例值、来源类型、覆盖率
    
- CodeGenOutput：代码片段、依赖说明、运行方式、关键参数
    

并且加一些“工程化字段”，让系统更稳定可排查：

- `schema_version`：避免未来 schema 演进互相不兼容
    
- `status` / `errors`：失败也要结构化（而不是一句“失败了”）
    
- `confidence` / `coverage`：支持主控做策略决策（例如 coverage 太低就回退重新发现）
    

---

### 3）验证失败后的“自愈重试”闭环（你要求的重点）

我实现的机制是：**主 Agent 永远不相信子 Agent 的裸输出**，必须验证通过才进入下一步。

流程是这样的（口播版）：

1. 子 Agent 输出 raw text（通常是 JSON 或 markdown 里的 JSON）
    
2. Orchestrator 用 Pydantic `model_validate` 校验
    
3. 如果 `ValidationError`：
    
    - Orchestrator 构造一个“修复提示”：包含
        
        - 校验失败的错误栈（字段缺失/类型错误/枚举不合法）
            
        - 正确的 schema 描述
            
        - 一份最小示例（few-shot）
            
    - 把 **raw 输出 + error + schema** 原封不动回传给同一个子 Agent，让它“只做格式修复/补全”
        
4. 允许重试 N 次（比如 2~3 次）：
    
    - N 次仍失败：**主 Agent 直接终止该分支**，给用户一个可理解的报错与建议（例如“字段 X 在缓存数据中未找到，建议提供样例页面/登录态/筛选条件”）
        

你可以用一句话总结这个机制的价值（面试特别加分）：

> “我把 LLM 的不确定输出，收敛成了强类型的可组合产物；失败也不会 silently fail，而是可控重试+最终可解释报错。”

---

## Result（结果）

- **稳定性**：跨 Agent 传递不再靠“约定俗成的 markdown”，而是强 schema，后续步骤不会因为格式飘了就崩
    
- **成功率提升**：子 Agent 输出不合格时自动修复，减少人工介入
    
- **可维护性**：新增子 Agent 只要定义 schema + 注册路由，主控的验证/重试框架复用
    
- **成本可控**：重试次数上限 + is_last_step 兜底，避免无限循环和 token 失控
    

---

# 简历点二（保持你原本优势，但补“格式产物”如何进入下一步）

## Situation

网站数据源多样（HTML/JSON/GraphQL）、动态渲染和懒加载普遍，传统人工写选择器慢且易失效。

## Task

搭建“数据发现 → 流量缓存 → 字段映射”的分层链路，让后续代码生成有可靠输入。

## Action（建议你这样讲，会更像工程闭环）

- **Discoverer** 用 Playwright + 网络监听捕获 API/HTML，并可滚动触发懒加载
    
- **DataCache** 把原始响应、清洗正文、截图摘要分层存储，保证后续 agent 可复用
    
- **FieldMapper** 基于缓存做搜索→生成 JSONPath/CSS→抽样验证
    
- **关键补充**：FieldMapper 的输出不是“描述性文本”，而是 **Pydantic 校验后的 FieldMappingOutput**，因此 CodeGenerator 能稳定消费：
    
    - 只要 schema 通过，CodeGenerator 就可以机械化地把 mapping 转成解析代码
        
    - 如果 schema 不通过，会触发上面那套“错误回传+重试修复”
        

## Result

- 自动化时间从 1-2 小时降到分钟级
    
- API 优先策略更稳定
    
- 字段覆盖率、成本控制（收敛策略）都有量化收益
    
### Situation（背景）

这是本项目最具挑战性的部分。在大模型应用中，最大的痛点是输出的不确定性。例如，我要求 Field Mapper Agent 生成 JSONPath 规则，但 LLM 可能会输出错误的语法，或者返回一段 Markdown 文本而不是纯 JSON。如果直接将这些错误数据传给下一个环节，会导致整个代码生成任务崩溃。

### Task（任务）

我的目标是构建一个**零容错的输出验证系统**：

1. **格式强制**：确保 Agent 100% 输出符合代码规范的结构化数据。
    
2. **逻辑自洽**：确保生成的规则在实际页面上能提取到数据。
    
3. **闭环修正**：当验证失败时，系统能自动引导 Agent 修正错误，而不是直接报错崩溃。
    

### Action（行动）

**1. 基于 Pydantic 的结构化约束** 我不再依赖 Prompt 来“恳求”模型输出 JSON，而是使用了 LangChain 的 `with_structured_output` 结合 Pydantic 模型。 例如，对于字段映射任务，我定义了严格的 Schema：

Python

```
class FieldMapping(BaseModel):
    field_name: str
    selector: str
    selector_type: Literal["css", "xpath", "regex"]
    confidence_score: float
```

**2. 验证-报错-重试循环（Validation Loop）** 我设计了一个标准的错误捕获与回传流程，这是保证稳定性的核心：

- **第一道防线（格式验证）**：当 Agent 输出内容后，系统立即尝试用 Pydantic 进行解析。如果解析失败（如缺少字段、类型错误），系统会捕获 `ValidationError`。
    
- **第二道防线（逻辑验证）**：即使格式正确，系统会运行一个轻量级验证器，尝试用生成的 Selector 在缓存的 HTML 中提取数据。如果提取结果为空，视为逻辑错误。
    
- **错误回传机制**：一旦触发上述任何错误，系统不会中断，而是将具体的错误信息（如 `Error: Field 'price' requires a float, got string` 或 `Error: Selector returns empty list`）封装成一条 `ToolMessage`，追加到对话历史中，并将控制权交还给该子 Agent。
    
- **Agent 自我修正**：子 Agent 看到报错信息后，会根据错误提示重新生成答案。这构成了一个微型的 ReAct 循环。
    

**3. 熔断与兜底策略** 为了防止 Agent 陷入死循环，我设置了 `max_retries=3`。如果连续三次修正失败，系统会触发熔断机制，抛出 `OptimizationFailure` 异常，并由主 Agent 捕获，告知用户“该页面结构过于复杂，请手动辅助”，从而避免系统卡死。

### Result（结果）

这一机制极大地提升了系统的鲁棒性。

1. **格式错误归零**：通过 Pydantic 强制约束，彻底消除了 JSON 解析失败的问题。
    
2. **代码可用性提升**：由于经过了逻辑验证，生成的爬虫代码一次运行成功率（One-Pass Success Rate）达到了 70% 以上。
    
3. **用户体验**：系统在后台默默完成了多次试错，用户看到的直接是经过验证的高质量结果。
---

# 简历点三（可观测性要讲“定位 schema 错误/重试链路”，更贴你新增重点）

## Situation

多 Agent 系统失败时很难定位：是路由错？工具错？还是输出格式错？

## Task

做全链路可观测，并建立数据驱动的 prompt/策略迭代。

## Action

- LangSmith tracing 记录每轮：路由决策、工具调用、子 Agent 原始输出、Pydantic 校验结果、重试次数
    
- 我会把失败归因分成三类（这句很加分）：  
    1）**工具/环境失败**（超时、反爬、登录态缺失）  
    2）**任务理解失败**（需求解析偏了）  
    3）**契约失败**（schema 不通过/字段缺失/类型不匹配）← 你要重点强调的
    
- 对第 3 类契约失败，可以直接从 trace 看到 ValidationError，并优化“修复提示模板”，降低重试次数
    

## Result

- 定位时间大幅缩短
    
- 提示词优化从“感觉调参”变成“基于失败类型的迭代”
    
- 让系统更像工程产品：可追踪、可回归、可解释
    
