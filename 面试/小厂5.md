
1. **用户注册功能（Java / SpringBoot，前端到后端全流程）**  
    1.1（基于你说自己做过 Java/SpringBoot）我现在需要一个**用户注册**功能，你从前端到后端把**完整实现过程**讲一下（你可以先思考、做笔记再讲）。  
    1.2（基于你刚讲的注册流程：前端提交用户名/手机号/密码/验证码，后端校验、加密入库、返回 cookie）你这个过程中**怎么保证手机号是唯一的**？有哪些手段？  
    1.3（基于你回答“数据库给手机号加唯一约束”）如果只在数据库里做唯一约束，作为业务方我怎么知道到底是**重复了**还是**没插进去**？  
    1.4 前端有时啥也拿不到提示——**这个是不是还得加点什么**？（基于“注册功能已经实现”这个前提）
    
2. **注册后立刻登录失败：如何处理与排查**  
    2.1 如果遇到场景：你刚注册了账号，用这个账号密码去登录，结果还是登录不了，你一般会怎么处理？**怎么检查问题**？  
    2.2（基于你说用 F12 看请求/错误码、查日志）你说具体一点：现在网页跑起来了，前后端都在本机运行，页面提示登录失败——你准备**怎么一步步排查**，看哪里出问题？
    
3. **在现有“手机号/用户名注册与登录”基础上扩展邮箱能力**  
    3.1 我刚才注册是用的**手机号和用户名两个字段**对吧？（基于“当前只有手机号/用户名注册与登录”）客户提新要求：想加一个类似**邮箱验证/邮箱登录**的功能，你打算怎么在现有结构上实现这个新要求？
    
4. **邮箱验证功能：后端怎么做 + 分层职责**  
    4.1（基于你一开始讲邮箱时偏快、且后端还没展开）你现在后端邮箱还没实现、刚才我们只做手机号对吧？那你先讲清楚：**邮箱验证功能在后端怎么做**（后面再考虑前端怎么传邮箱）。  
    4.2（基于你回答“数据库加 Email 字段、增加绑定邮箱接口、验证码校验、策略模式改造登录”等）那你后端的几个层（Controller / Service 等）分别要做哪些内容？你刚刚只说了数据库。
    
5. **多 Agent 协作：如何防止幻觉导致信息不一致**  
    5.1（基于你说任务在多个 Agent 之间路由、依赖大模型总结与生成选择器/代码）你怎么保证某个 Agent **出幻觉**时，两边拿到的信息仍然是**匹配的**？不然他随机生成 A，你这边当成 B，系统就崩了。  
    5.2（追问上题）你中间没有做一些**逻辑上的限制/校验**之类的东西吗？  
    5.3 我给你一个思路（固定输出格式、做格式/字段对等校验等），你觉得**能不能用**、能不能改进你的方案？  
    5.4 如果这一关（格式/字段对等检查）都过不了，那你的结果是不是要考虑**本身就有问题**？