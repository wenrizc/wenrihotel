
1. 假设有一个从抖音抓取出来的 JSON 文件，包含 1 万多条评论，但格式很乱（含 HTML 标签、emoji、换行等）。需要把好评和差评分类，并存到飞书表格里。你用 Python 会怎么处理？会用到哪些库？爬取或数据清洗时常用哪些库？
    
2. 抓取小红书/抖音时，经常遇到动态加载、元素 ID 随机生成。比如小红书 PC 端需要加载 token 或某些参数才能打开页面。遇到这种情况你会怎么做？请说明反爬、鉴权参数、动态渲染的处理思路。
    
3. 你在做项目时，Supervisor 怎么知道爬虫 agent 的任务已经完成？在做 agent 的过程中，你用过哪些架构或逻辑框架（例如某种 Agent 编排/框架思想）？
    
4. 在 Supervisor 架构下，如果爬虫 agent 连续三次获取数据失败，Supervisor 应该怎么决策？是直接报错停止，还是切换备用数据源？请说明失败重试、降级兜底、决策策略。
    
5. 我们业务有个需求：把一篇 2000 字或 1000 多字的小红书爆款笔记压缩成一个 JSON 对象，并包含痛点、种草关键词、竞品品牌词。你会怎么写 prompt？这个 prompt 一般包含哪些部分？
    
6. 奶粉场景：知识库只有产品适合 3 岁及以上，但用户问适合 1 岁宝宝吗？模型可能会幻觉回答可以。这种情况你怎么优化流程或 prompt 来杜绝？请从 RAG、知识边界控制、防幻觉策略角度回答。