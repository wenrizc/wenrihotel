
## 1）开场

面试官您好，我在BDA博达克咨询主要负责为咨询项目提供数据支持服务，参与各类抓数项目编写爬虫脚本，同时完成ETL这类数据清洗工作，清洗异源数据，迁移过往数据，计算各类业务指标，支持咨询部门工作的展开，同时完善数据监控，部分需要入库的项目会进行数据定时的检验，实现邮件告警等。在此期间也引入了大模型对非结构化文本解析，并进行数据打标分类，提高了工作效率。


第二是 ETL 数据清洗与指标体系搭建。

针对多源异构数据（如京东和天猫订单状态口径不一）的问题，我基于 Node.js 搭建了清洗框架，统一了数据标准。在此基础上，我负责计算 GMV、AOV（平均客单价）以及复杂的 Cohort 留存率分析，产出标准化的 CSV 报表，直接支持咨询顾问进行市场规模和用户行为分析。

第三是数据质量监控体系。

为了应对源站改版导致的脏数据问题，我设计了一套监控系统。它能在数据入库前进行字段空值和逻辑一致性校验，并定时对比近两天的数据量波动。一旦波动超过阈值（如暴跌 50%），系统会自动触发邮件告警。这套机制将发现异常的时间从天级缩短到了分钟级。

第四是 LLM 大模型应用探索。

针对正则无法处理的非结构化文本（如从复杂标题中提取品牌、评论情感评分），我引入了 OpenAI API，通过设计 Few-Shot 提示词工程，实现了高准确率的品牌提取和 1-5 分的细粒度情感打分，大幅降低了人工标注成本。

整体来说，这段经历让我不仅掌握了爬虫逆向和数据清洗的技术，更重要的是培养了我从数据质量到业务交付的闭环思维。

---

## 第二部分：深挖模块准备（STAR 详细复盘）

当面试官对某个环节感兴趣追问时，请使用以下逻辑进行展开。

### 模块一：Web/App 数据采集与风控对抗

核心难点：指纹检测、参数加密、高频封禁

技术细节展开：

1. 流量分析与逆向：
    
    我使用 Charles 分析 App 和 Web 流量。以 58 同城为例，我定位到请求头中包含一个基于设备信息的 fingerprint 加密字段。我阅读了混淆的 JS 代码，还原了其基于时间戳、UA 和设备 ID 的 MD5 生成逻辑，并在 Node.js 中重写了该算法。
    
2. 浏览器指纹模拟：
    
    针对 WebDriver 检测，我使用 Puppeteer 配合 Stealth 插件，核心操作是重写 navigator.webdriver 属性为 false，同时模拟真实的 UA、屏幕分辨率和鼠标移动轨迹，欺骗服务端的机器人检测。
    
3. 代理池与并发控制：
    
    我实现了基于城市的代理轮换策略，并使用 Bottleneck 库控制并发速率（如每秒请求不超过 3 次），配合代理鉴权签名，最终将 IP 封禁率降低了 80%。
    
#### **1. S（情境）**

- **项目目标**：我们需要批量、自动化地抓取Shopee平台上大量店铺的详细数据，包括店铺评分摘要等关键业务信息，并要求系统能够7x24小时稳定运行。
- **主要挑战**：Shopee部署了业界领先的反爬体系，主要包括：
    - **请求签名校验**：每个请求都需要一个由前端JavaScript动态生成的、有时效性的签名。
    - **CSRF令牌与防爬Cookie**：如`csrftoken`和`SPC_SI`等，是会话合法性的关键。
    - **严格的速率与并发限制**：单个IP或会话请求过快会立即被拦截。
    - **代理IP风控**：对数据中心代理有很强的识别和封禁能力。

#### **2. T（任务）**

我的核心任务是**设计并实现一套“可持续、低封禁”的抓取方案**。这不仅仅是写一个爬虫，更是要构建一个完整的对抗系统，确保每次请求的**签名、Cookie、头部指纹、请求节奏**都与真实浏览器行为高度一致。

#### **3. A（行动）**

我采取了一套分层、解耦的策略来系统性地解决这些问题：

**a. 签名与请求头仿真（解决核心校验）**

- **逆向与封装**：我们首先逆向分析了关键的`shoppe_v2.js`文件，定位了签名生成函数。我将这个逻辑封装成一个独立的服务函数 `generateSignEntry(url)`，它能够动态生成包含 `x-api-source`、`x-csrftoken`、`af-ac-enc-dat` 等关键字段的签名头部。
    - _源码支撑_：正如在 `shoppe_v2.js` 的14376-14382行所实现的算法，我们确保了签名的正确性。
- **动态注入**：在每次发起业务请求前，都会调用该函数，为当前URL生成全新的签名头，并与固定的浏览器特征头（如UA、`sec-ch-ua`）合并，确保每次请求的指纹都是有效且新鲜的。
    - _源码支撑_：在 `shopee_shopOrSth.js` 的67-73行，可以看到在请求构造器中动态调用 `get_headers(url)` 来装配最终请求头。

**b. Cookie与CSRF令牌的生命周期管理（解决会话状态）**

- **独立获取流程**：我们没有在业务请求中硬编码Cookie，而是设计了一个独立的“预热”流程。首先，用一个专门的爬虫模块去访问Shopee的安全接口，以“首次访客”的身份合法获取一整套初始Cookie（包括`SPC_SI`和`csrftoken`）。
    - _源码支撑_：`get_ck.js` 的60-141行完整展示了这个模拟浏览器首次访问、解析Set-Cookie、建立合法会话的过程。
- **集中分发与复用**：获取到的Cookie和CSRF令牌会被存入中央存储。工作节点（Worker）在执行抓取任务前，会先从这里领取一套新鲜的“身份凭证”，然后将其注入到任务上下文中。这样避免了每个任务都重复走登录流程，极大降低了成本和触发风控的风险。
    - _源码支撑_：`worker.js` 的68-134行展示了Worker如何从远端服务获取凭证，并将其作为 `cookie_csrftoken` 等参数带入任务。

**c. 代理池与智能速率控制（解决IP限制）**

- **双层速率控制**：
    1. **爬虫实例级**：在每个爬虫实例配置中，我们设置了严格的 `rateLimit: 1000ms` 和 `maxConnection: 1`，从根源上限制单个会话的请求速度。
    2. **任务级随机化**：在任务调度层，我们引入了随机延迟（limiter），进一步打散请求节奏，避免形成可预测的时间模式。
- **动态代理调度**：我们集成了内部代理池。在爬虫的 `schedule` 生命周期钩子中，动态地为每个请求分配不同的代理IP，实现请求的IP轮询。
    - _源码支撑_：`shopee_shopOrSth.js` 的21-73行展示了代理选择逻辑；`get_ck.js` 的26-54行展示了如何在调度钩子中注入代理配置。

**d. 任务编排与系统健壮性**

- **基因去重**：每个抓取店铺的任务都有一个唯一的“基因”（gene），通常就是店铺ID。系统会维护一个已处理基因的分桶集合（`_hasDone`），在调度前进行过滤，防止重复抓取同一店铺导致请求频率异常升高。
    - _源码支撑_：`shopee_shopOrSth.js` 的131-190行包含了基因生成和去重检查的逻辑。
- **优雅失败与重试**：数据采用流式写入CSV，避免内存溢出。对于因网络或临时风控导致的失败请求，系统会将其自动重新推入队列，等待后续重试，从而提升了整体任务的成功率。

### 模块二：ETL 数据管道与业务指标计算

核心难点：多平台口径统一、复杂聚合计算

技术细节展开：

1. 统一数据口径：
    
    我处理的最大痛点是不同平台的定义差异。例如京东的 已完成 和天猫的 交易成功 需要映射为统一的 completed 状态。我建立了一套映射字典，在清洗阶段将所有异构字段转化为标准字段。
    
2. 复杂指标实现：
    
    在计算留存率（Cohort Analysis）时，我编写了逻辑复杂的聚合脚本。首先通过 GroupBy 确定每个用户的首次购买月份作为群组标识，然后构建时间窗口矩阵，计算不同群组在后续月份的复购率。为了方便顾问观察衰减趋势，我还实现了数据的右对齐展示格式。
    
3. 工具应用：
    
    使用 Ramda 库进行函数式编程，提升代码可读性；处理了 CSV 导出的 BOM 头问题，防止 Excel 打开中文乱码。
    

### 模块三：数据质量监控与告警

#### Situation（情境）

在实习初期，我们的监控是被动的。往往是咨询顾问发现报表里的数据是空的，或者价格字段全是 0，才反馈给我们。这时候再去排查，往往已经丢失了一两天的关键数据。 这就带来了两个痛点：第一，发现问题的延迟太高；第二，传统的 `setInterval` 或 `Cron` 在面对数百个高频任务时，会消耗大量 CPU 资源，且难以动态管理。

#### Task（任务）

我的目标是搭建一套主动式的监控体系，它需要满足三个要求：

1. **高性能调度**：能够高效地管理成百上千个监控任务的触发。
    
2. **通用告警**：提供统一的报警接口，不用每个爬虫都写一遍发邮件的代码。
    
3. **业务适配性**：虽然监控系统是通用的，但必须能针对不同项目（如天猫的 GMV 算法 vs. 抖音的直播流）配置特有的业务校验规则。
    

#### Action（行动）

为了解决这些问题，我主要做了三部分的技术建设：

**第一，封装基于时间轮（Time Wheel）的高性能定时系统。** 我没有使用简单的原生定时器，而是自己封装了一个 TimeWheel 类。 我设计了一个环形数组结构，通过游标每秒移动一格来触发任务。对于需要延迟执行或周期性检查的监控脚本，我只需要以 O(1)的时间复杂度将它们插入到对应的“槽位”中。 这样做的好处是，无论监控任务增加到多少，CPU 的消耗基本是恒定的，避免了大量定时器抢占资源导致 Event Loop 阻塞的问题。这套时间轮系统成为了整个监控体系的“心脏”。

**第二，搭建标准化的邮件告警服务。** 我封装了一个通用的 `Notification Service`。它不仅仅是发邮件，我还加入了“告警分级”和“防骚扰”机制。 比如，对于“解析失败”这种 Fatal 级别的错误，系统会立即发送高优先级邮件；而对于“数据波动 5%”这种 Warning 级别，系统会先在内存中聚合，积累一定数量或时间后再汇总发送。这样既保证了关键问题不漏，又避免了因为网络抖动导致邮箱被轰炸。

**第三，构建基于业务规则的数据校验系统。** 这是最核心的应用层。我将监控逻辑抽象为“入库前”和“入库后”两道防线，并支持针对不同项目配置规则：

- **入库前**：我会在 Pipeline 中注入校验逻辑。比如采集天猫数据时，我配置了“价格 > 0”和“店铺名非空”的规则，一旦不满足，直接拦截并触发告警。
    
- **入库后**：我利用时间轮每天定时触发 SQL 统计任务。比如针对“美团外卖”项目，我会对比昨天和今天的店铺总量，如果下跌超过 20%，系统就会自动判定为异常。 这种设计做到了“底层通用，上层灵活”，不同项目的业务规则只需通过配置文件或简单的逻辑函数即可挂载到监控系统中。
    

#### Result（结果）

这套系统上线后，带来了显著的效果：

1. **时效性提升**：我们将数据异常的发现时间从平均 24 小时缩短到了 15 分钟以内。
    
2. **稳定性增强**：通过时间轮调度，即使在高峰期运行上百个监控任务，服务器的 CPU 占用率也保持在很低的水平。
    
3. **交付质量**：脏数据入库率降低了 90% 以上，大大增加了咨询团队对数据的信任度，减少了后期的反复沟通和清洗成本。
    

---

### 面试官可能的追问（预判与准备）

**Q1: 你提到的时间轮具体是怎么实现的？为什么要用它？** **答：** 我是用一个数组来模拟钟表盘，比如长度 60 代表 60 秒。通过一个指针每秒移动一格。如果任务要在 5 秒后执行，就算出 `(当前指针 + 5) % 60` 的下标，把任务放进去。 为什么要用它？因为如果用大量的 `setTimeout`，Node.js 需要在内部维护一个庞大的定时器链表，插入和删除效率会变低。而时间轮的插入和触发都是 O(1) 的，非常适合高并发的定时任务场景。

**Q2: 针对不同项目的业务规则，你是怎么解耦的？** **答：** 我采用了“策略模式”的思想。监控系统本身只负责调度和发邮件，具体的校验逻辑（比如怎么算 GMV 异常）被封装成独立的 `Validator` 函数或配置文件。监控系统运行时，会根据任务 ID 动态加载对应的 Validator。这样增加新项目时，我只需要写校验规则，不需要改动监控系统的核心代码。

**Q3: 邮件告警如果太多把邮箱炸了怎么办？** **答：** 我在封装邮件模块时做了一个简单的“熔断”和“聚合”机制。如果是同一个错误 ID 在短时间内连续触发，系统会合并这 5 分钟内的所有报错，只发一封汇总邮件，并在邮件标题注明 `[聚合告警 x 50次]`，这样既能引起注意，又不会淹没收件箱。

### 模块四：LLM 在清洗中的应用

### S 情境

在电商平台中商品标题、店铺名等非结构化数据用规则很难覆盖：品牌抽取、品类归类、情感评分效果不稳定，人工标注成本高。

### T 任务

用LLM在以下任务中验证增益：

- 品牌抽取
    
- 品类归类到标准类目
    
- 评论情感 1–5 分评分 + 关键词抽取  
    并同时控制成本、保证输出可用。
    

### A 行动

- **提示词工程：**明确角色、约束输出格式（JSON）、加入高质量Few-shot样例与边界案例。
    
- **混合策略控成本：**规则先过滤简单样本，难例才调用LLM；对高频标题做缓存；必要时批处理。
    
- **质量闭环：**把错误案例回流到样例里迭代Prompt，提升稳定性。
    

### R 结果（你可保留你原有量化，但建议口径更严谨）

- 品牌抽取准确率、品类覆盖率显著优于纯规则方案；评论评分与人工一致性提升。
    
- 人工标注与清洗成本下降，数据处理速度更快。
    
- 同一套Prompt框架可迁移到其它文本数据源，扩展性较好。
    

**追问备选**

- 如何评估准确率与一致性？抽样标注怎么做？
    
- 如何处理模型幻觉与不在类目表的输出？
    
- 为什么选这个模型、如何做降本？

