
核心思想是：不要一上来堆技术细节，而是按固定顺序交付一个“能跑通核心功能”的系统，再在最后做扩展、性能、容错等深挖。文档本身推荐的流程是：Requirements → Core Entities → API/System Interface → Optional Data Flow → High Level Design → Deep Dives。

## 一、总流程概览

| 阶段              |       建议时间 | 目标                    | 主要产出                                |
| --------------- | ---------: | --------------------- | ----------------------------------- |
| 1. 需求澄清         |     约 5 分钟 | 明确题目边界，锁定核心功能和系统质量要求  | 功能需求 + 非功能需求 + 必要估算                 |
| 2. 核心实体         |     约 2 分钟 | 明确系统中的核心对象            | User、Post、Message、Order 等实体列表       |
| 3. API / 系统接口   |     约 5 分钟 | 定义系统对外契约              | REST / GraphQL / RPC / WebSocket 接口 |
| 4. 数据流，可选       |     约 5 分钟 | 适用于爬虫、流处理、消息系统等长流程场景  | 从输入到输出的处理步骤                         |
| 5. 高层设计 HLD     | 约 10–15 分钟 | 画出端到端架构，先保证核心链路跑通     | 服务、数据库、缓存、队列、对象存储等                  |
| 6. 深挖 Deep Dive |    约 10 分钟 | 针对瓶颈、扩展性、一致性、延迟、容错做优化 | 分片、缓存、异步化、限流、监控等优化方案                |

---

## 二、阶段 1：需求澄清，约 5 分钟

### 1. 功能需求 Functional Requirements

用句式：

> 用户 / 客户端应该能够……

目标是把题目收敛到**最核心的 3 个功能**，不要列太多。文档强调，场景题系统往往有很多功能，但面试中重点是识别并优先处理最重要的几个功能。

例如设计 Twitter：

```text
用户可以发布 tweet
用户可以关注其他用户
用户可以查看关注用户的 feed
```

例如设计 Cache：

```text
客户端可以写入 item
客户端可以设置过期时间
客户端可以读取 item
```

面试话术：

```text
我先确认一下核心需求，避免设计范围过大。
我会优先聚焦 3 个主要功能：A、B、C。
其他功能例如 X、Y，我先标记为 out of scope，后面有时间再讨论。
```

---

### 2. 非功能需求 Non-functional Requirements

非功能需求要具体、量化、结合场景，而不是泛泛地说“高可用、低延迟、可扩展”。

错误表达：

```text
系统要低延迟。
```

更好的表达：

```text
feed 拉取延迟应低于 200ms。
搜索结果返回应低于 500ms。
系统需要支持 100M+ DAU。
```

可按这个清单检查：

|维度|面试中要问的问题|
|---|---|
|CAP|这个系统更重一致性还是可用性？|
|Environment|是否有移动端、弱网、低内存、低电量等限制？|
|Scalability|读多还是写多？是否有突发流量？|
|Latency|哪些请求必须低延迟？目标是多少？|
|Durability|数据丢失是否可接受？|
|Security|是否涉及认证、授权、隐私、加密？|
|Fault Tolerance|单点失败、重试、降级、灾备怎么处理？|
|Compliance|是否涉及支付、金融、医疗、隐私法规？|

可以记成：

```text
CAP / Environment / Scalability / Latency / Durability / Security / Fault Tolerance / Compliance
```

---

### 3. 容量估算 Capacity Estimation

文档的建议是：**不要为了估算而估算**。只有当估算会直接影响设计选择时才做。

例如这些情况值得估算：

```text
QPS 会不会影响是否需要缓存？
数据量会不会影响是否需要分库分表？
TopK 的候选集合是否能放进单机内存？
消息吞吐是否需要 Kafka 分区？
对象大小是否决定要不要使用 blob/object storage？
```

面试话术：

```text
我先不做完整 back-of-the-envelope 估算。
如果后面某个设计选择依赖 QPS、存储量或吞吐量，我会在对应位置补充估算。
```

---

## 三、阶段 2：核心实体，约 2 分钟

这一阶段不要展开完整数据模型，只列出系统中的关键名词。文档强调，核心实体是 API 会交换、系统会持久化的核心对象。

思考方式：

```text
系统里的 actor 是谁？
核心资源 / nouns 是什么？
哪些对象需要持久化？
哪些对象会出现在 API 里？
```

例如 Twitter：

```text
User
Tweet
Follow
Feed
```

例如 WhatsApp：

```text
User
Conversation
Message
Device
DeliveryReceipt
```

例如 Ticketmaster：

```text
User
Event
Venue
Seat
Reservation
Ticket
Payment
```

面试话术：

```text
我先列一个第一版核心实体，后面画架构时如果发现状态变化需要更多字段或实体，再迭代补充。
```

---

## 四、阶段 3：API / 系统接口，约 5 分钟

API 是系统和用户之间的契约，用来反推后面的服务和存储设计。

默认选择：

```text
大多数产品型场景题：REST
不同客户端有复杂数据选择需求：GraphQL
服务间高性能调用：RPC / gRPC
实时通信：WebSocket / Server-Sent Events
```

文档建议：不要过度纠结协议选择，大多数场景默认 REST 即可；实时功能再额外补 WebSocket 或 SSE。

例如 Twitter：

```http
POST /v1/tweets
body: {
  "text": "..."
}

GET /v1/tweets/{tweetId}

POST /v1/follows
body: {
  "followee_id": "..."
}

GET /v1/feed
```

注意点：

```text
资源名用复数：/tweets，而不是 /tweet
当前用户从 auth token 里取，不要从 body 里相信 user_id
敏感身份信息必须来自认证上下文
```

面试话术：

```text
我会先定义核心 API，因为后面的高层设计可以逐个 API 推导数据流和组件。
```

---

## 五、阶段 4：数据流，可选，约 5 分钟

不是所有题都需要这一阶段。适用于：

```text
Web Crawler
视频上传与转码
日志聚合
支付流程
消息投递
广告点击聚合
TopK / Trending
异步任务调度
```

例如 Web Crawler：

```text
1. 获取 seed URLs
2. 抓取 HTML
3. 解析页面
4. 提取链接
5. 存储内容和新 URL
6. 重复调度
```

例如视频上传：

```text
1. 用户上传视频
2. 写入对象存储
3. 发送转码任务
4. Worker 转码多种清晰度
5. 更新视频状态
6. CDN 分发播放
```

面试话术：

```text
这个系统有比较明显的多阶段处理链路，我先用一个简化数据流说明输入如何变成输出，再进入架构设计。
```

---

## 六、阶段 5：高层设计 HLD，约 10–15 分钟

这一阶段的目标是先画出**能满足核心 API 的简单端到端架构**。不要过早加入复杂优化。文档明确提醒：候选人常见错误是太早堆复杂度，导致最后没有交付完整系统。

推荐画法：

```text
Client
  ↓
Load Balancer / API Gateway
  ↓
Application Service
  ↓
Database / Cache / Message Queue / Object Storage
```

更具体一点：

```text
Client
  ↓
API Gateway
  ↓
Auth Service
  ↓
Core Service
  ↓
Primary DB

可选：
Cache
Message Queue
Worker
Search Index
Object Storage
CDN
Notification Service
```

设计顺序建议：

```text
按 API 一个一个推导架构。
每个 API 说明：
1. 请求进来经过哪些组件？
2. 读写了哪些状态？
3. 写入哪个数据库 / 缓存 / 队列？
4. 返回什么结果？
```

面试话术：

```text
我先画一个简单但完整的端到端设计，确保核心功能可以工作。
缓存、队列、分片这些优化点我会先标记出来，在 deep dive 阶段展开。
```

---

## 七、阶段 6：深挖 Deep Dive，约 10 分钟

高层设计完成后，最后用 10 分钟围绕非功能需求加固系统。文档列出的深挖目标包括：满足非功能需求、处理边界情况、识别瓶颈、根据面试官追问改进设计。

常见深挖方向：

| 深挖主题 | 典型问题            | 常见方案                             |
| ---- | --------------- | -------------------------------- |
| 扩展读  | feed、搜索、详情页读多   | Cache、CDN、Read Replica、预计算       |
| 扩展写  | 高并发写入、秒杀、消息发送   | Queue、分片、批处理、异步写                 |
| 一致性  | 支付、库存、订单、关注关系   | 事务、幂等、锁、版本号、SAGA                 |
| 低延迟  | feed、搜索、聊天      | 缓存、索引、预聚合、就近部署                   |
| 大文件  | 图片、视频、文档        | Object Storage、CDN、分片上传          |
| 实时性  | 聊天、评论、直播、通知     | WebSocket、Pub/Sub、Kafka          |
| 容错   | 服务挂掉、消息重复、DB 故障 | 重试、DLQ、降级、限流、熔断                  |
| 热点问题 | 大 V、热门活动、抢票     | Sharding、热点拆分、fanout 策略          |
| 可观测性 | 怎么知道系统工作正常      | Metrics、Logging、Tracing、Alerting |

例如 Twitter feed 的深挖：

```text
问题：实时生成 feed 太慢。
方案 1：fanout-on-read，读时聚合关注用户的 tweet。
方案 2：fanout-on-write，发布时提前写入粉丝 feed inbox。
权衡：大 V 用户写扩散成本高，普通用户可以 fanout-on-write，大 V 可以 fanout-on-read 或混合策略。
```

---

## 八、面试中被提前打断深挖时怎么处理

附件评论区也提到一个常见情况：面试官可能在你还没画完整 HLD 时，就要求深入某个点，比如 WhatsApp 的一对一聊天、Kafka 分区、离线消息等。比较稳妥的策略不是硬顶回去，而是先回答面试官的问题，再尝试把讨论拉回主线。

可用话术：

```text
这个点确实很关键。我先给一个简短判断：
这里我会倾向于使用 Kafka / persistent queue，因为我们需要处理异步投递和离线用户。

我先把这个点标记为 deep dive。
为了保证端到端设计完整，我想先用 2 分钟把主链路画完，然后我们可以回到这个点深入 Kafka 分区、离线消息和扩展性。
```

如果面试官坚持深挖：

```text
好的，那我们就聚焦这个链路。
我会从数据模型、写入路径、分区策略、失败恢复、扩容方式几个方面展开。
如果最后还有时间，我再快速补全整体 HLD。
```

---

## 九、可以直接背的场景题答题模板

```text
1. 我先澄清需求。
   功能上，我会聚焦 A、B、C 三个核心能力。
   非功能上，我认为最关键的是 X、Y、Z，例如延迟、可用性、扩展性。

2. 我先列核心实体。
   这个系统里主要有 Entity1、Entity2、Entity3。
   这里只列第一版，后续设计中再补字段和关系。

3. 我定义核心 API。
   默认用 REST；如果有实时需求，再补 WebSocket。
   API 包括：
   - POST ...
   - GET ...
   - PUT ...
   - DELETE ...

4. 如果这是多阶段系统，我先描述数据流。
   输入经过步骤 1、2、3，最终产出结果。

5. 我画一个简单完整的高层设计。
   Client → API Gateway → Service → DB。
   对于异步任务加入 Queue + Worker。
   对于大文件加入 Object Storage + CDN。
   对于搜索加入 Search Index。
   对于高读加入 Cache。

6. 最后 deep dive。
   我会回到非功能需求，逐个处理：
   - 如何扩展读写
   - 如何降低延迟
   - 如何保证一致性
   - 如何处理失败和重试
   - 如何监控和告警
```

---

## 十、最终压缩版流程

```text
需求澄清
→ 核心实体
→ API / 接口
→ 可选数据流
→ 高层架构
→ 深挖优化
```

面试时最重要的不是把所有技术都讲完，而是先交付一个**完整、可工作的基础系统**，再根据非功能需求和面试官追问逐步加复杂度。